ADWIO M3U -> XTREAM BRIDGE FIX

Purpose:
- M3U URLs generated by Xtream panels (get.php?username=...&password=...)
  now use the existing working Xtream API path internally.
- UI still identifies the source as M3U.
- Account details are populated from player_api.php.
- Live / Movies / Series use the same proven Xtream loading path.
- Raw/generic M3U URLs still fall back to the normal M3U parser.
- No provider or test credentials are hard-coded in the source.
