name: Apply ADWIO 5.0.3 Orientation Sports Final

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify package
        run: test -f ADWIO_5.0.3_ORIENTATION_SPORTS_FINAL.zip

      - name: Extract update
        shell: bash
        run: |
          set -euo pipefail
          TMP_DIR="${RUNNER_TEMP}/adwio503"
          rm -rf "$TMP_DIR"
          mkdir -p "$TMP_DIR"
          unzip -q ADWIO_5.0.3_ORIENTATION_SPORTS_FINAL.zip -d "$TMP_DIR"
          test -d "$TMP_DIR/ADWIO-main/app"

          rsync -a \
            --exclude='.git/' \
            --exclude='.github/workflows/' \
            --exclude='ADWIO_5.0.3_ORIENTATION_SPORTS_FINAL.zip' \
            "$TMP_DIR/ADWIO-main/" ./

      - name: Verify version and orientation
        run: |
          grep -q 'versionCode = 26' app/build.gradle.kts
          grep -q 'versionName = "5.0.3"' app/build.gradle.kts
          grep -q 'screenOrientation="sensorLandscape"' app/src/main/AndroidManifest.xml

      - name: Commit and push
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "Apply ADWIO 5.0.3 orientation and sports final"
          git push
